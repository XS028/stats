This program gets a price values for all pairs on BTC-e.com and builds a google visualization line chart. Just set the timeout in config.ini file, start it for a 20 (for example) hours and press STOP button. The chart will be opened in your default browser

This program requires C:\stats folder with its files.